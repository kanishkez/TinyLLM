batch_size: 8
grad_accum: 8
epochs: 3
lr: 3e-4
min_lr: 3e-5
warmup_steps: 500
max_grad_norm: 1.0
dtype: bfloat16
compile: true
optimizer: adamw
dataset_path: datasets/processed/train.bin
val_path: datasets/processed/val.bin
tokenizer_path: tokenizer/tokenizer.model
save_path: checkpoints/
log_every: 10
eval_every: 500
save_every: 1000
